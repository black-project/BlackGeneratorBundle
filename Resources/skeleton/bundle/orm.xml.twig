<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <!--
    <parameters>
{% block parameters %}
        <parameter key="{{ extension_alias }}.infrastructure.manager.model.class">{{ namespace }}\Infrastructure\Doctrine\ExampleManager</parameter>
{% endblock parameters %}
    </parameters>

    <services>
{% block services %}

        <service id="{{ extension_alias }}.infrastructure.manager.model" class="%{{ extension_alias }}.infrastructure.manager.model.class%">
            <argument type="service" id="{{ extension_alias }}.entity.manager" />
            <argument>%{{ extension_alias }}.model.class%</argument>
        </service>

        <service id="{{ extension_alias }}.entity.manager" alias="doctrine.orm.entity_manager" />

{% endblock services %}
    </services>
    -->
</container>
